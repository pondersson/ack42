// Global parameters
$fn = 16;         // number of facets used when drawing arcs
cut_delta = 0.01; // small nudge to help with rendering
kerf = 0.0;       // laser cutting kerf

u = 19.05;        // measured 18.75 (?) on helidox, cherry spec is 19.05
su_t = 3.0;       // switch unit (plate) thickness
sh = 14.0 - kerf; // switch hole side length
